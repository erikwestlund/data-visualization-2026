---
title: "1. Pre-Class Directions"
format:
  html:
    embed-resources: true
    toc: true
---

# 1. Data Visualization Day 1 Requirements

This course will use R and `ggplot2` as the backbone of our work. We will use R to load, clean, summarize, and analyze data. We will use `ggplot2` and related libraries to make the visualizations.

Before class, make sure you can open, run, and render a Quarto notebook. You do not need to be an expert before class, but we want to make sure you have a working setup.

# Software Requirements

Please set up one of these integrated development environments, or IDEs:

- [RStudio](https://posit.co/download/rstudio-desktop)
- [Positron](https://positron.posit.co/download.html)

You also need:

- [R](https://cran.r-project.org/)
- the R packages used in the course, installed from [CRAN](https://cran.r-project.org/)
- [Quarto](https://quarto.org/docs/get-started/) (RStudio & Positron install a working version; you'll only need to look here if Quarto is not already working through RStudio or Positron)


If you are not sure which IDE to choose, use RStudio. It is the most popular R editor and has the most tutorials, answers, and classroom examples available.

Positron is a newer, more modern editor from Posit. I use Positron for much of my own work. It works well for this course, but in-class examples will be shown in RStudio because it is still the most common student setup.

Either RStudio or Positron will work; under the hood, it's all R.

# Install R

Install R first. R is the programming language itself. RStudio and Positron are editors that use R, but they do not replace R.

Download R here:

<https://cran.r-project.org/>

Choose the installer for your operating system.

- Windows: choose `Download R for Windows`, then install the current base version of R.
- macOS: choose `Download R for macOS`, then install the current `.pkg` file for your computer. 
- Linux: choose your Linux distribution and follow the CRAN instructions.

After installing R, restart RStudio or Positron if either one is already open.

# Install RStudio

RStudio Desktop is the safest choice if you are unsure which editor to use.

Download RStudio Desktop here:

<https://posit.co/download/rstudio-desktop>

Choose the installer for your operating system.

- Windows: download the Windows installer and run it.
- macOS: download the macOS installer, open it, and move RStudio into Applications.
- Linux: download the installer for your Linux distribution.

Open RStudio after installation. You should see an R Console. If RStudio cannot find R, install R first and restart RStudio.

# Install Positron

Positron is also fine for this course. It is newer and more modern, and it feels closer to other modern code editors.

Download Positron here:

<https://positron.posit.co/download.html>

On the download page, check the box to accept the license agreement. The installer links are disabled until you accept the license agreement.

Choose the installer for your operating system.

- Windows: download the Windows `.exe` installer and run it.
- macOS with Apple Silicon: download the `arm64.dmg` file.
- macOS with Intel: download the `x64.dmg` file.
- Linux: download the `.deb` or `.rpm` installer that matches your distribution.

Open Positron after installation. If Positron asks you to choose a runtime or interpreter, choose R.

# Install Quarto

Quarto is what turns `.qmd` scientific notebooks into HTML files you can share with your colleagues.

RStudio and Positron ship with their own version of Quarto bundled. I highly recommend using this version. You only need to install Quarto directly if Quarto is missing, out of date, or not working when you try to render.

Download Quarto here:

<https://quarto.org/docs/get-started/>

After installing Quarto, restart RStudio or Positron.

# Get The Course Project

The course materials are in this GitHub repository:

<https://github.com/erikwestlund/data-visualization-2026>

You have two options.

Option 1: download the project as a ZIP file.

1. Open <https://github.com/erikwestlund/data-visualization-2026>.
2. Click the green `Code` button.
3. Choose `Download ZIP`.
4. Unzip the downloaded file.
5. Move the unzipped folder somewhere you can find it again.

Option 2: clone the project with Git, if you already know how to use Git.

```text
https://github.com/erikwestlund/data-visualization-2026.git
```

Then open the course project:

1. In RStudio, open `data-visualization-course.Rproj` from the course folder.
2. In Positron, open the course folder.

The project includes the data, lesson notebooks, rendered slides, rendered assignment instructions, and syllabus we will use in class.

# Install Packages

Run this once in the R Console if you do not already have these packages installed:

```{r}
#| eval: false
install.packages(c("dplyr", "ggplot2", "readr", "stringr", "here"))
```

The render check only uses `dplyr` and `ggplot2`, but the full course will also use the other packages listed above.

# Run The Setup Check

To check your setup, open this file inside the course project:

```text
modules/00_preclass-tech-check/02_setup-check.qmd
```

Then:

1. Run the code chunks one at a time.
2. Click `Render`.
3. Confirm that an HTML file is created.
4. Open the HTML file and confirm that the table and plot appear.

If the setup-check file renders, your basic setup is ready for class.

# If Something Fails

If something does not work, do not panic. Bring the error message to class or send it to the instructor.

The most useful information is:

- your operating system
- whether you are using RStudio or Positron
- the exact error message
- whether the error happened when running a chunk or when rendering
